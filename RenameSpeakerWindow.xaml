<Window x:Class="AudioScript.RenameSpeakerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Speaker"
        Icon="pack://application:,,,/assets/AudioScript.ico"
        Width="420"
        MinWidth="360"
        MinHeight="210"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="72"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="From"
                       VerticalAlignment="Center"
                       Opacity="0.78"/>
            <TextBlock x:Name="FromSpeakerText"
                       Grid.Row="0"
                       Grid.Column="1"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"/>

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="0,12,0,0"
                       Text="To"
                       VerticalAlignment="Center"
                       Opacity="0.78"/>
            <TextBox x:Name="ToSpeakerTextBox"
                     Grid.Row="1"
                     Grid.Column="1"
                     Margin="0,12,0,0"
                     MinHeight="30"
                     VerticalContentAlignment="Center"
                     TextChanged="ToSpeakerTextBox_TextChanged"/>
        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button MinWidth="110"
                    MinHeight="28"
                    Padding="12,6"
                    Tag="&#xE711;"
                    Style="{StaticResource ToolbarButtonStyle}"
                    Margin="0,0,8,0"
                    Content="Cancel"
                    Click="CancelButton_Click"/>
            <Button x:Name="RenameButton"
                    MinWidth="110"
                    MinHeight="28"
                    Padding="12,6"
                    Tag="&#xE73E;"
                    Style="{StaticResource ToolbarPrimaryButtonStyle}"
                    Content="Rename"
                    Click="RenameButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
