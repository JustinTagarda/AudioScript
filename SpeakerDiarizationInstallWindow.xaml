<Window x:Class="AudioScript.SpeakerDiarizationInstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Preparing Speaker Detection"
        Icon="pack://application:,,,/assets/AudioScript.ico"
        Width="480"
        MinWidth="440"
        MinHeight="250"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Text="Preparing Speaker Detection"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="1"
                   Margin="0,10,0,0"
                   FontSize="13"
                   TextWrapping="Wrap"
                   Text="Checking speaker detection components."/>

        <Grid Grid.Row="2"
              Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       FontSize="11"
                       Opacity="0.78"
                       Text="Download"/>
            <ProgressBar x:Name="DownloadProgressBar"
                         Grid.Row="1"
                         Margin="0,5,0,10"
                         Height="8"
                         Minimum="0"
                         Maximum="100"
                         Value="0"/>

            <TextBlock Grid.Row="2"
                       FontSize="11"
                       Opacity="0.78"
                       Text="Install"/>
            <ProgressBar x:Name="InstallProgressBar"
                         Grid.Row="3"
                         Margin="0,5,0,0"
                         Height="8"
                         Minimum="0"
                         Maximum="100"
                         Value="0"/>

            <TextBlock x:Name="DetailText"
                       Grid.Row="4"
                       Margin="0,8,0,0"
                       FontSize="11"
                       Opacity="0.76"
                       TextWrapping="Wrap"
                       Text="Waiting to start."/>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    MinWidth="96"
                    MinHeight="32"
                    Padding="12,6"
                    Tag="&#xE711;"
                    Style="{StaticResource ToolbarButtonStyle}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
